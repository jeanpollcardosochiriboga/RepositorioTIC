DASH_PORT=1883
DASH_IP=192.168.1.10           # IP del servidor (respaldo del QR; en la Pi se autodetecta)

# Arma — workers concurrentes del attacker
WORKERS=50                     # Bajar a 30 en Raspberry Pi 3B+

# URL inicial del objetivo (modo sin protección)
TARGET_URL=http://esc3-target:5000/reservar
